name: ci

on:
  push:
    branches: [main]
  pull_request:

jobs:
  lint-test:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        python-version: ["3.11", "3.12"]
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: ${{ matrix.python-version }}
          cache: pip
      - name: Install
        run: |
          python -m pip install --upgrade pip
          pip install -e ".[dev]"
      - name: Ruff lint
        run: ruff check src tests scripts
      - name: Ruff format check
        run: ruff format --check src tests scripts
      - name: Mypy
        run: mypy src/methodos
      - name: Schema drift check
        run: |
          python scripts/regenerate_schema.py --out /tmp/schema_check.json
          diff -u schemas/method_schema.json /tmp/schema_check.json
      - name: Tests
        run: pytest -q
      - name: Integration tests parse-only
        run: |
          # Guards against integration tests that no longer import or collect.
          # pytest exits 5 when nothing matches the marker — there are no
          # integration tests yet, and that is not a failure. Any other
          # non-zero exit (collection error) still fails the job.
          pytest --collect-only -m integration || [ "$?" -eq 5 ]
